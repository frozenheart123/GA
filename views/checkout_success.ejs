<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Payment Received — Mala Hot Pot Market</title>
  <style>
    :root{--primary:#d14343;--ring:#e5e7eb;--ink:#0f172a;--muted:#6b7280;--card:#fff;--radius:16px;--shadow:0 10px 30px rgba(15,23,42,.08);--space:18px;--max:1020px}
    *{box-sizing:border-box}
    html,body{height:100%}
    body{margin:0;font-family:system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;color:var(--ink);background:linear-gradient(180deg,#fff,#fff 55%,#fdf6f6)}
    .container{max-width:var(--max);margin:0 auto;padding:32px}
    .card{background:var(--card);border-radius:var(--radius);box-shadow:var(--shadow);border:1px solid var(--ring);padding:32px}
    h1{margin-top:0}
    .summary{margin-top:18px;display:flex;flex-direction:column;gap:8px}
    .summary-row{display:flex;justify-content:space-between}
    .btn{margin-top:18px;display:inline-flex;align-items:center;gap:8px;padding:12px 18px;border-radius:12px;border:1px solid var(--ring);background:#fff;color:var(--primary);font-weight:700;cursor:pointer}
  </style>
</head>
<body>
  <main class="container">
    <div class="card">
      <h1>Payment confirmed</h1>
      <p>Thank you for your order, <strong><%= paymentInfo.name %></strong>.</p>
      <% if (paymentInfo.method) { %>
        <p class="note">Payment method: <strong><%= paymentInfo.method %></strong></p>
      <% } %>
      <% if (paymentInfo.reference) { %>
        <p class="note">Reference: <strong><%= paymentInfo.reference %></strong></p>
      <% } %>
      <p class="note">We've emailed the receipt to <%= paymentInfo.email || 'the address you provided' %>.</p>
      <div class="summary">
        <div class="summary-row"><span>Subtotal</span><strong>$<%= totals.subtotal.toFixed(2) %></strong></div>
        <div class="summary-row"><span>Member Cash Back (5%)</span><strong>-$<%= (isMember ? totals.cashback.toFixed(2) : '0.00') %></strong></div>
        <div class="summary-row"><span>Shipping</span><span>1–3 days</span></div>
        <div class="summary-row" style="font-weight:700"><span>Total</span><strong>$<%= totals.total.toFixed(2) %></strong></div>
      </div>
      <% if (orderId) { %>
        <a class="btn" href="/orders/<%= orderId %>">View Receipt</a>
      <% } else { %>
        <a class="btn" href="/orders">View My Orders</a>
      <% } %>
    </div>
  </main>
</body>
</html>

