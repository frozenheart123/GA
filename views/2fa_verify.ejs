<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Verify 2FA - Mala Hot Pot Market</title>
  <style>
    :root{ --primary:#d14343; --primary-ink:#8b1f1f; --ink:#0f172a; --muted:#6b7280; --card:#ffffff; --soft:#f6f7fb; --ring:#e5e7eb; --accent:#fef3f3; --radius:16px; --shadow:0 10px 30px rgba(15,23,42,.08); --space:18px; --max:1160px; }
    *{box-sizing:border-box} html,body{height:100%}
    body{ margin:0; font-family:system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif; font-size:18px; line-height:1.6; color:var(--ink); background:linear-gradient(180deg,#fff, #fff 45%, #fdf6f6 100%); }
    a{color:var(--primary);text-decoration:none}
    .container{max-width:var(--max);margin:0 auto;padding:24px;display:flex;justify-content:center;align-items:center;min-height:calc(100vh - 120px)}
    .navbar{ position:sticky; top:0; z-index:20; background:rgba(255,255,255,.9); backdrop-filter:saturate(180%) blur(8px); border-bottom:1px solid var(--ring); }
    .navwrap{ max-width:var(--max); margin:0 auto; display:flex; gap:16px; align-items:center; justify-content:space-between; padding:14px 24px }
    .brand{display:flex;align-items:center;gap:10px;font-weight:900;letter-spacing:.3px;color:var(--primary-ink); font-size:1.15rem;}
    .brand svg{width:28px;height:28px}
    .navlinks{display:flex;gap:16px;align-items:center;flex-wrap:wrap}
    .navlinks a{color:var(--ink);opacity:.9; font-size:1.05rem;}
    .badge{ padding:7px 14px; border-radius:999px; background:#ffe6e6; color:var(--primary-ink); font-weight:800; border:1px solid #ffd1d1; font-size:1rem; }
    .card{ background:var(--card); border:1px solid var(--ring); border-radius:var(--radius); box-shadow:var(--shadow); padding:30px 26px; max-width:520px; margin:28px auto; }
    .qr-region{ display:flex; flex-direction:column; align-items:center; gap:12px; margin:12px 0 20px; }
    .qr-region img{ border-radius:16px; border:1px solid var(--ring); background:#fff; }
    .qr-region small{ color:#64748b; font-size:.85rem; }
    .input{ width:100%; border:1px solid var(--ring); background:#fff; padding:12px 14px; border-radius:12px; font-size:1rem; margin-top:6px }
    .label{ font-weight:800 }
    .btn{ padding:12px 16px; border-radius:12px; border:1px solid var(--ring); background:#fff; cursor:pointer; font-weight:800; font-size:1rem; }
    .btn.primary{ background:#d53b3b; color:#fff; border-color:#d53b3b }
    .row{display:flex;gap:12px;flex-wrap:wrap;align-items:center}
    .error{ color:#b91c1c; font-weight:800; margin:12px 0 }
    footer{margin-top:32px;border-top:1px solid var(--ring); background:#fff}
    .foot{max-width:var(--max);margin:0 auto;padding:22px 24px;color:var(--muted);font-size:1rem;display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap}
  </style>
</head>
<body>
  <header class="navbar">
    <div class="navwrap">
      <div class="brand">
        <svg viewBox="0 0 24 24" fill="none"><path d="M6 12c3.5-3.5 6-2 10-6 1.6-1.6 3-1.5 3 0 0 2-2 4-3 5-5 5-7 8-11 8-2.5 0-3.5-1.5-3-3 1-3 2-3 4-4z" stroke="#d14343" stroke-width="1.5" fill="#ffdddd"/><path d="M15 6c0-2 1-3 3-3" stroke="#8b1f1f" stroke-width="1.5" stroke-linecap="round"/></svg>
        <span>Mala Hot Pot Market</span>
      </div>
      <nav class="navlinks">
        <% const showCustomerLinks = !user || user.role !== 'admin'; %>
        <a href="/">Home</a>
        <a href="/menu">Menu</a>

        <% if (showCustomerLinks) { %>
          <a href="/cart">Cart</a>
          <a href="/membership">Membership</a>
        <% } %>
        <% if (user && user.role !== 'admin') { %>
          <a href="/orders">My Orders</a>
        <% } %>
        <% if (user && user.role === 'admin') { %>
          <a href="/admin">Admin</a>
        <% } %>

        <a href="/register">Register</a>
        <a href="/login">Login</a>
      </nav>
      <div class="badge" id="member-badge"><%= (user && user.role==='admin') ? "Admin" : "User" %></div>
    </div>
  </header>

  <main class="container">
    <div class="card">
      <h1 style="margin:0 0 8px 0; font-size:1.4rem">Two-Factor Verification</h1>
      <% if (typeof error !== 'undefined' && error) { %><div class="error"><%= error %></div><% } %>
      <% if (showQRCode && qrDataUrl) { %>
        <div class="qr-region">
          <img src="<%= qrDataUrl %>" alt="Scan with Authenticator" width="220" height="220">
          <small>Use Google Authenticator, Authy, or any TOTP app to scan.</small>
        </div>
      <% } %>
      <form method="post" action="/2fa/verify">
        <label class="label">6-digit code</label>
        <input class="input" name="token" inputmode="numeric" pattern="[0-9]{6}" required>
        <div class="row" style="justify-content:space-between;margin-top:16px">
          <button class="btn primary" type="submit">Verify & Continue</button>
          <a href="/login">Back to login</a>
        </div>
      </form>
    </div>
  </main>

  <footer>
    <div class="foot">
      <div>&copy; <%= new Date().getFullYear() %> Mala Hot Pot Market</div>
      <div>Prototype — SG</div>
    </div>
  </footer>
</body>
</html>
